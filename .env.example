# Puerto donde se expone la API HTTP
PORT=3000

# Ruta del archivo JSON utilizado como base de datos.
# Para producción se recomienda inyectar este valor como un secreto o variable protegida.
DATABASE_FILE=./data/database.json

# Nivel de detalle de los logs: error | warn | info | debug
LOG_LEVEL=info

# URL del webhook para recibir notificaciones cuando se crea un mensaje de contacto.
# Configurar como un secreto en GitHub Actions (CONTACT_WEBHOOK_URL) o en el orquestador que se utilice.
CONTACT_WEBHOOK_URL=
